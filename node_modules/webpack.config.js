const path = require('path');

module.exports = {
  entry: './stundenplan/stundenplan.js', // Der Einstiegspunkt deiner Anwendung
  output: {
    filename: 'bundle.js', // Der Name der gebündelten Datei
    path: path.resolve(__dirname, 'dist'),
  },
  mode: 'production', // oder 'production' für Produktions-Bundles
};
